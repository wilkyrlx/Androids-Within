

function JoinDetails({ setView }: { setView: any }) {
    return (
        <div id="join-details">
            <input id="join-code" type="text" placeholder="Enter Game Code" />
            <button>Join Game</button>
        </div>
    )
}

export default JoinDetails;